# Security Rules
- Never write API keys, tokens, or passwords to files
- Never commit .env files or credential files
- Check for secrets before git add
- Don't expose internal URLs or infrastructure details
